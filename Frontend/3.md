# 🚀 NexSkill Frontend 3-Developer Work Distribution & Prompts

This document outlines the branch strategy, module assignments, and ready-to-use **Antigravity AI Prompts** for your 3 frontend developers: **Adrija**, **Saif**, and **Diganta**.

---

# 🔀 Git Branch & Integration Strategy

To ensure seamless parallel development without merge conflicts using **Redux Toolkit (RTK)** and **React Router DOM**:

1. **`adrija-frontend` (Adrija - Core Lead)**: Sets up the main layout, root router, RTK Store structure, Auth, Dashboard Overview, ATS Resume Analyzer, and Credential Tracker.
2. **`saif_frontend` (Saif - Market & Curriculum)**: Builds Labour Market Intelligence, Industry Curriculum, Skill Gap Detection, Dynamic Feedback, and Demand Forecasting.
3. **`diganta_frontend` (Diganta - Employer & Career/Trainer)**: Builds Employer Discovery Panel, AI Career Guidance, Trainer Development, and Shared Custom Hooks/Utilities.

> 💡 **RTK Convention Rule:** Each developer works strictly inside their designated `redux/slices/<feature>Slice.js` and `api/<feature>Api.js`. When merging into `main`, simply register all reducers in `redux/store.js`.

---

# 📊 Team Work Breakdown Overview

| Developer & Branch | Core Assigned Modules | Assigned Files & Redux Slices |
| :--- | :--- | :--- |
| **Adrija**<br>`adrija-frontend`<br>*(Core Lead)* | <ul><li>Core Layouts & Shell Navigation</li><li>Redux Store Root</li><li>Auth System</li><li>Main Dashboard Overview</li><li>AI ATS Resume Analyzer</li><li>Progress & Credential Tracker</li></ul> | <ul><li>`layouts/*`, `components/common/*`</li><li>`routes/*`, `context/ThemeContext.jsx`</li><li>`redux/store.js`, `redux/slices/authSlice.js`, `resumeSlice.js`, `progressSlice.js`</li><li>`pages/auth/*`, `pages/dashboard/*`</li><li>`pages/resumeAnalyzer/*`, `pages/progressTracker/*`</li><li>`api/authApi.js`, `resumeAnalyzerApi.js`, `progressTrackerApi.js`</li><li>`components/charts/ProgressChart.jsx`, `forms/ResumeUploadForm.jsx`</li></ul> |
| **Saif**<br>`saif_frontend` | <ul><li>Labour-Market Intelligence</li><li>Industry-Aligned Curriculum</li><li>Skill Gap & Dynamic Feedback</li><li>Future-Ready Demand Forecasting</li></ul> | <ul><li>`pages/marketIntelligence/*`</li><li>`pages/curriculum/*`</li><li>`pages/demandForecasting/*`</li><li>`api/marketIntelligenceApi.js`, `curriculumApi.js`, `demandForecastApi.js`</li><li>`redux/slices/marketSlice.js`, `curriculumSlice.js`, `demandSlice.js`</li><li>`components/charts/MarketDemandChart.jsx`, `SkillGapChart.jsx`</li><li>`components/forms/FeedbackForm.jsx`</li></ul> |
| **Diganta**<br>`diganta_frontend` | <ul><li>Employer Discovery Panel</li><li>AI Career Guidance</li><li>Trainer Development Panel</li><li>Custom Hooks & Global Utils</li></ul> | <ul><li>`pages/employerPanel/*`</li><li>`pages/careerGuidance/*`</li><li>`pages/trainerDevelopment/*`</li><li>`api/employerApi.js`, `careerGuidanceApi.js`, `trainerDevApi.js`</li><li>`redux/slices/employerSlice.js`, `careerSlice.js`, `trainerSlice.js`</li><li>`hooks/*`, `utils/*`</li></ul> |

---

# 🤖 Prompts for Each Developer's AI (Copy & Paste)

Copy each prompt into the developer's Antigravity AI assistant chat session.

---

### 🟢 Prompt 1: Adrija (`adrija-frontend`) — Core Lead & Student Hub

```markdown
You are pair programming with Adrija on the NexSkill Frontend Prototype for SIH 2026 (Problem Statement 26134).

YOUR BRANCH: `adrija-frontend`

PROJECT CONTEXT:
NexSkill bridges Labour Market Demand -> Skill Gap Detection -> AI Resume Analyzer -> Credential Tracker -> Job Readiness.

TECH STACK: React 19, Vite, Tailwind CSS v4, Redux Toolkit (RTK), React Router DOM, Axios, Recharts, Lucide React icons.

ASSIGNED FILES & RESPONSIBILITIES:
1. Core Shell & Navigation:
   - `src/layouts/MainLayout.jsx`, `AuthLayout.jsx`, `DashboardLayout.jsx`
   - `src/components/common/` (`Navbar.jsx`, `Sidebar.jsx`, `Footer.jsx`, `Modal.jsx`, `Card.jsx`, `Button.jsx`, `Loader.jsx`)
   - `src/context/ThemeContext.jsx`
   - `src/routes/AppRoutes.jsx`, `ProtectedRoute.jsx`
2. RTK Root Store & Auth:
   - `src/redux/store.js` (Setup configureStore registering auth, resume, progress, market, curriculum, demand, employer, career, trainer slices)
   - `src/redux/slices/authSlice.js`, `resumeSlice.js`, `progressSlice.js`
   - `src/api/authApi.js`, `resumeAnalyzerApi.js`, `progressTrackerApi.js`
3. Pages & Features:
   - `src/pages/auth/Login.jsx`, `Register.jsx`
   - `src/pages/dashboard/DashboardOverview.jsx` (Metric cards, quick navigation, summary stats)
   - `src/pages/resumeAnalyzer/AtsResumeAnalyzer.jsx` & `src/components/forms/ResumeUploadForm.jsx`
   - `src/pages/progressTracker/CredentialTracker.jsx` & `src/components/charts/ProgressChart.jsx`

EXECUTION PLAN (Break into 4 Sessions):
- Session 1: Build Core Layouts, Navbar, Sidebar, ThemeContext, AppRoutes router setup, and RTK `store.js`.
- Session 2: Build Auth API/Slice and Login/Register UI.
- Session 3: Build DashboardOverview with metric summary cards and stats widgets.
- Session 4: Implement ATS Resume Analyzer (with score gauge & feedback) and Credential Progress Tracker (with Recharts progress visualization).

Rules:
- Build polished, state-of-the-art UI with Tailwind CSS v4, dark mode support, glassmorphism, and Lucide icons.
- Ensure RTK asyncThunks or slices cleanly export state for full state management.
- Do not create backend code (Node/Express/MongoDB). Use simulated REST API calls or Axios services with mock data responses.

Start by presenting your plan for Session 1.
```

---

### 🔵 Prompt 2: Saif (`saif_frontend`) — Market & Curriculum Intelligence

```markdown
You are pair programming with Saif on the NexSkill Frontend Prototype for SIH 2026 (Problem Statement 26134).

YOUR BRANCH: `saif_frontend`

PROJECT CONTEXT:
NexSkill analyzes Labour Market Demand trends, detects skill gaps in real-time, generates industry-aligned dynamic curriculums, and forecasts emerging job market requirements.

TECH STACK: React 19, Vite, Tailwind CSS v4, Redux Toolkit (RTK), React Router DOM, Axios, Recharts, Lucide React icons.

ASSIGNED FILES & RESPONSIBILITIES:
1. Labour Market Intelligence:
   - `src/pages/marketIntelligence/MarketIntelligence.jsx`
   - `src/api/marketIntelligenceApi.js`
   - `src/redux/slices/marketSlice.js`
   - `src/components/charts/MarketDemandChart.jsx` (Recharts area/bar chart for industry demand)
2. Industry-Aligned & Dynamic Curriculum:
   - `src/pages/curriculum/IndustryCurriculum.jsx` (Curriculum catalog aligned with industry standards)
   - `src/pages/curriculum/DynamicCurriculum.jsx` (Real-time skill gap detection & adaptive learning path)
   - `src/api/curriculumApi.js`
   - `src/redux/slices/curriculumSlice.js`
   - `src/components/charts/SkillGapChart.jsx` (Radar/bar chart for target vs current student skills)
   - `src/components/forms/FeedbackForm.jsx` (Industry feedback submission on curriculum)
3. Future-Ready Demand Forecasting:
   - `src/pages/demandForecasting/DemandForecasting.jsx` (5-year skill demand projections)
   - `src/api/demandForecastApi.js`
   - `src/redux/slices/demandSlice.js`

EXECUTION PLAN (Break into 3 Sessions):
- Session 1: Implement Labour-Market Intelligence page with interactive filter controls, industry trends grid, and MarketDemandChart visualization.
- Session 2: Implement Industry-Aligned & Dynamic Curriculum pages, SkillGapChart radar visualization, and industry FeedbackForm.
- Session 3: Implement Future-Ready Demand Forecasting page with predictive trend charts and emerging tech role insights.

Rules:
- Export clean RTK slice reducers (`marketSlice.reducer`, `curriculumSlice.reducer`, `demandSlice.reducer`) and actions so they register effortlessly into `redux/store.js`.
- Design visual charts using Recharts and style with modern Tailwind CSS v4.
- Do not create backend code (Node/Express/MongoDB). Use Axios API service layer with clean mock data fallbacks.

Start by presenting your plan for Session 1.
```

---

### 🔴 Prompt 3: Diganta (`diganta_frontend`) — Employer Panel & AI Guidance/Trainer Dev

```markdown
You are pair programming with Diganta on the NexSkill Frontend Prototype for SIH 2026 (Problem Statement 26134).

YOUR BRANCH: `diganta_frontend`

PROJECT CONTEXT:
NexSkill connects job-ready candidates with employers through an Employer Discovery Panel, provides personalized AI Career Guidance recommendations, and upskills trainers through Industry-Driven Trainer Development.

TECH STACK: React 19, Vite, Tailwind CSS v4, Redux Toolkit (RTK), React Router DOM, Axios, Recharts, Lucide React icons.

ASSIGNED FILES & RESPONSIBILITIES:
1. Employer Discovery Panel:
   - `src/pages/employerPanel/EmployerDiscovery.jsx` (Candidate search, match score, direct talent sourcing)
   - `src/api/employerApi.js`
   - `src/redux/slices/employerSlice.js`
2. AI Career Guidance:
   - `src/pages/careerGuidance/AiCareerGuidance.jsx` (Interactive AI career assistant, learning roadmaps, role recommendations)
   - `src/api/careerGuidanceApi.js`
   - `src/redux/slices/careerSlice.js`
3. Industry-Driven Trainer Development:
   - `src/pages/trainerDevelopment/TrainerDevelopment.jsx` (Faculty upskilling programs, industry workshops, certifications)
   - `src/api/trainerDevApi.js`
   - `src/redux/slices/trainerSlice.js`
4. Custom Hooks & Shared Utilities:
   - `src/hooks/useAuth.js`, `useFetch.js`, `useDebounce.js`
   - `src/utils/constants.js`, `formatters.js`

EXECUTION PLAN (Break into 3 Sessions):
- Session 1: Implement Shared Custom Hooks & Utils (`useAuth`, `useFetch`, `useDebounce`, `constants.js`, `formatters.js`).
- Session 2: Implement Employer Discovery Panel with candidate filters, match score badges, and talent profiles.
- Session 3: Implement AI Career Guidance hub and Trainer Development panel.

Rules:
- Export clean RTK slice reducers (`employerSlice.reducer`, `careerSlice.reducer`, `trainerSlice.reducer`) and actions for `redux/store.js`.
- Use Lucide icons, rich badges, search/filter controls, and modern Tailwind CSS v4 styling.
- Do not create backend code (Node/Express/MongoDB). Use Axios service layer with realistic mock data endpoints.

Start by presenting your plan for Session 1.
```

---

# 🚀 Workflow Sequence for Team Integration

1. **Step 1:** **Adrija** initializes branch `adrija-frontend`, builds Session 1 (layouts, root router, RTK store), and pushes to GitHub.
2. **Step 2:** **Saif** (`saif_frontend`) and **Diganta** (`diganta_frontend`) pull `main` / Adrija's layout structure.
3. **Step 3:** Each developer inputs their prompt into their Antigravity AI assistant to complete their assigned sessions.
4. **Step 4:** Merge all three feature branches into `main`. The `redux/store.js` setup in `main` registers all slices:
   ```javascript
   import { configureStore } from '@reduxjs/toolkit';
   import authReducer from './slices/authSlice';
   import resumeReducer from './slices/resumeSlice';
   import progressReducer from './slices/progressSlice';
   import marketReducer from './slices/marketSlice';
   import curriculumReducer from './slices/curriculumSlice';
   import demandReducer from './slices/demandSlice';
   import employerReducer from './slices/employerSlice';
   import careerReducer from './slices/careerSlice';
   import trainerReducer from './slices/trainerSlice';

   export const store = configureStore({
     reducer: {
       auth: authReducer,
       resume: resumeReducer,
       progress: progressReducer,
       market: marketReducer,
       curriculum: curriculumReducer,
       demand: demandReducer,
       employer: employerReducer,
       career: careerReducer,
       trainer: trainerReducer,
     },
   });
   ```